//https://leetcode.com/problems/convert-binary-number-in-a-linked-list-to-integer/description/?envType=problem-list-v2&envId=linked-list

/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val === undefined ? 0 : val)
 *     this.next = (next === undefined ? null : next)
 * }
 */

/**
 * @param {ListNode} head
 * @return {number}
 */
var getDecimalValue = function(head) {
    let num = 0;

    while (head !== null) {
        // Left shift num by 1 and add current bit
        num = (num * 2) + head.val;
        head = head.next;
    }

    return num;
};

//Time: O(n), space: O(1)
//Traverse the list and keep building the decimal value using num = num * 2 + currentBit.